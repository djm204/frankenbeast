{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["FRANKENBEAST_SEED"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/scripts/vitest-*.ts",
        "$TURBO_ROOT$/packages/*/src/**"
      ],
      "env": ["INTEGRATION", "E2E", "EVAL", "DOCKER_BUILD"]
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/scripts/vitest-*.ts",
        "$TURBO_ROOT$/packages/*/src/**"
      ],
      "outputs": ["coverage/**"],
      "env": ["INTEGRATION", "E2E", "EVAL", "DOCKER_BUILD"]
    },
    "test:live": {
      "dependsOn": ["build"],
      "cache": false,
      "env": ["FBEAST_LIVE_BENCH_E2E"]
    },
    "test:integration": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/scripts/vitest-*.ts",
        "$TURBO_ROOT$/packages/*/src/**"
      ],
      "env": ["INTEGRATION"]
    },
    "test:eval": {
      "cache": false,
      "env": ["EVAL"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/eslint.workspace.config.js"]
    }
  }
}
