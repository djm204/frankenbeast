# Progress Doc Rule

- [x] Inspect the current repo guidance and task-tracking files that govern agent workflow.
- [x] Confirm whether an existing progress-document convention already exists.
- [x] Create a task-specific progress document for this task before making persistent workflow edits.
- [x] Update persistent guidance so every assigned task requires a matching `tasks/<name-of-task>-progress.md` checklist.
- [x] Record the rule in `tasks/lessons.md` and `tasks/todo.md`.
- [x] Re-read the changed files, verify the wording is consistent, and record the completion note here.

## Acceptance Criteria

- Every assigned task now has an explicit persistent rule requiring a matching progress document.
- The required progress document name and location are defined consistently.
- The rule is recorded both in project guidance and in lessons to reinforce future compliance.

## Notes

- This file exists because the user made progress persistence non-negotiable for all assigned tasks.
- Verification complete on 2026-04-27: `AGENTS.md`, `tasks/lessons.md`, and `tasks/todo.md` all reflect the same `tasks/<name-of-task>-progress.md` rule.
