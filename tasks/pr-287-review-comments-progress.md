# PR 287 Review Comments Progress

- [x] Fetch unresolved PR #287 review threads with thread resolution state.
- [x] Identify the actionable `timeout` portability regression in generated pre-tool hooks.
- [x] Add failing regression tests for missing `timeout` behavior.
- [x] Implement minimal generated hook fail-open handling for missing `timeout`.
- [x] Run focused tests and typechecks.
- [x] Reply to and resolve addressed review threads.
- [x] Push fixes and trigger another Codex review with `@codex`.
- [ ] Repeat the `@codex` review-fix-comment-resolve-trigger cycle three total times, stopping early only if a cycle returns no actionable comments.

## Review

- Cycle 1: Addressed missing `timeout` portability feedback by treating status `127` like timeout statuses `124`/`137` in generated Codex and Gemini pre-tool hooks. Added regression tests for both clients with a status-127 `timeout` shim. Verified `rtk npm test -- --run src/cli/hook-scripts.test.ts` passed with 11 tests and `npm run typecheck` passed in `packages/franken-mcp-suite`. Replied to and resolved thread `PRRT_kwDORezACM6AMRhD`.
- Cycle 2: New Codex review identified `timeout` wrapper status `125` and `126` as additional fail-open cases. Added Codex and Gemini red tests for both statuses before changing production code; the red phase failed with status `2`. Updated generated pre-hooks to fail open for timeout-wrapper statuses `124|125|126|127|137`. Verified `rtk npm test -- --run src/cli/hook-scripts.test.ts` passed with 15 tests and `npm run typecheck` passed in `packages/franken-mcp-suite`. Replied to and resolved thread `PRRT_kwDORezACM6AVNna`.
- Cycle 3: New Codex review identified project `.codex/` taking precedence over same-project JSON client dirs. Added a red test proving project `.gemini/` should beat project `.codex/` while preserving the earlier project-Codex-over-home-JSON case; the red phase returned `codex` instead of `gemini`. Moved project `.codex/` detection after project JSON-client detection but before home-level fallback. Verified `rtk npm test -- --run src/cli/hook-scripts.test.ts src/cli/mcp-client-paths.test.ts src/cli/uninstall-entrypoint.test.ts src/cli/uninstall.test.ts` passed with 35 tests and `npm run typecheck` passed in `packages/franken-mcp-suite`. Replied to and resolved thread `PRRT_kwDORezACM6AV2_r`.
- Cycle 4: Third Codex trigger returned two new threads: stop fail-open on pre-tool status `137`, and validate explicit `--client=` values. Added red tests for both before changing production code; the red phase failed on status `137` fail-open and invalid `--client=codez`. Removed `137` from pre-tool fail-open statuses and added shared `parseMcpClient()` validation for the main and uninstall entrypoints. Verified `rtk npm test -- --run src/cli/hook-scripts.test.ts src/cli/uninstall-entrypoint.test.ts src/cli/main.test.ts src/cli/mcp-client-paths.test.ts src/cli/uninstall.test.ts` passed with 39 tests and `npm run typecheck` passed in `packages/franken-mcp-suite`. Replied to and resolved threads `PRRT_kwDORezACM6AXCae` and `PRRT_kwDORezACM6AXCai`.
- Cycle 5: New Codex review identified that status `127` from GNU `timeout` can mean the wrapped `fbeast-hook` command is missing, not just that `timeout` itself is unavailable. Updated generated pre-tool hooks to check `command -v timeout` before invocation and fail open only when `timeout` is absent or returns wrapper statuses `124|125|126`; status `127` from the wrapped governor now denies. Added isolated Codex/Gemini/Claude hook tests for missing `timeout`, missing `fbeast-hook`, and Claude generated JSON-stdin hooks. Verified `rtk npm test --` passed with 136 tests, `rtk npm run build` passed, and `rtk npm run typecheck` passed in `packages/franken-mcp-suite`. Replied to and resolved thread `PRRT_kwDORezACM6Adznw`, pushed the branch, and triggered another `@codex review`.
- Cycle 6: New Codex review identified two actionable threads: legacy Claude `preToolCall`/`postToolCall` hook entries were not removed during uninstall, and generated Claude hook command paths were not quoted for projects with spaces in their path. Added regression coverage for both cases. Updated uninstall hook filtering to remove top-level fbeast hook entries as well as nested matcher entries, and shell-quoted generated Claude hook script paths. Verified `rtk npm test -- --run src/cli/init.test.ts src/cli/uninstall.test.ts` passed with 39 tests, `rtk npm test --` passed with 138 tests, `rtk npm run typecheck` passed, and `rtk npm run build` passed in `packages/franken-mcp-suite`. Replied to and resolved threads `PRRT_kwDORezACM6ArmFB` and `PRRT_kwDORezACM6ArmFH`, then pushed the branch.
